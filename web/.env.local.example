NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# Backend-only — used to mint ephemeral Realtime tokens for the mobile app.
# Must come from the EU-data-residency-enabled OpenAI project (Phase 0).
OPENAI_API_KEY=
# https://api.openai.com while the EU project is pending approval;
# switch to https://eu.api.openai.com once Phase 0 is done (ADR 0001).
OPENAI_API_BASE_URL=https://api.openai.com
